#summary Procedure to install Berkeley DB for Mac
== Berkeley DB ==

Oracle Berkeley DB is the industry-leading open source, embeddable database engine that provides developers with fast, reliable, local persistence with zero administration. Oracle Berkeley DB is a library that links directly into your application.


== Installing ==

Download the latest stable version of the Berkeley DB from the [http://www.oracle.com/technology/software/products/berkeley-db/index.html link] to your home directory. Open a shell and extract downloaded files typing:

{{{
tar -xvf db-4.8.24.tar
}}}

Change to the build directory with:
{{{
cd db-4.8.24/build_unix
}}}

To configure the Berkeley DB on release mode type the following command:
{{{
../dist/configure --enable-cxx --prefix=/usr/local
make
make install
}}}

Now, before start creating debug library clean all. Type: 
{{{
make clean
}}}

And then generate debug library: 
{{{
../dist/configure --enable-cxx --enable-debug --prefix=/usr/local
make
make install
}}}


[InstallingForMac Mac Configuration] | [http://code.google.com/p/tonatiuh/w/list Wiki Home]